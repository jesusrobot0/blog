import { EB_Garamond } from "next/font/google";
import { Chivo } from "next/font/google";

export const primaryFont = EB_Garamond({ subsets: ["latin"] });
export const LogoFont = Chivo({ subsets: ["latin"] });
